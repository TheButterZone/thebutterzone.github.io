name: Process Submission

permissions:
  contents: write
  issues: write

on:
  issues:
    types: [labeled]

jobs:
  update-data:
    # Only run when approve label is added
    if: github.event.label.name == 'approve'

    runs-on: ubuntu-latest

    steps:

      # ---------------- CHECKOUT ----------------
      - name: Checkout repository
        uses: actions/checkout@v4

      # ---------------- NODE 24 ----------------
      - name: Setup Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: 24.x

      # ---------------- DETECT MODE ----------------
      - name: Detect mode
        id: mode
        run: |
          BODY="${{ github.event.issue.body }}"

          if echo "$BODY" | grep -qi "remove"; then
            echo "mode=remove" >> $GITHUB_OUTPUT
          else
            echo "mode=add" >> $GITHUB_OUTPUT
          fi

          echo "MODE: $(cat $GITHUB_OUTPUT)"

      # ---------------- PARSE ISSUE (OPTION A FIX) ----------------
      - name: Parse submission issue
        id: parse
        uses: stefanbuck/github-issue-parser@v3
        with:
          template-path: .github/ISSUE_TEMPLATE/submit-info.yml

      # ---------------- PARSE REMOVE ISSUE (OPTION A FIX) ----------------
      - name: Parse removal issue
        id: parse_remove
        uses: stefanbuck/github-issue-parser@v3
        with:
          template-path: .github/ISSUE_TEMPLATE/remove-info.yml

      # ---------------- PROCESS DIRECTORY ----------------
      - name: Process directory changes
        run: node scripts/process-directory.js
        env:
          MODE: ${{ steps.mode.outputs.mode }}
          USERNAME: ${{ steps.parse.outputs.username || steps.parse_remove.outputs.username }}
          BTC: ${{ steps.parse.outputs.bitcoin }}

      # ---------------- DEBUG OUTPUT ----------------
      - name: Debug data output
        run: |
          echo "==== data.json ===="
          cat data.json || echo "missing data.json"

          echo "==== .changed ===="
          cat .changed || echo "missing .changed"

      # ---------------- READ CHANGE FLAG ----------------
      - name: Read change flag
        id: change_flag
        run: |
          if [ -f .changed ]; then
            echo "changed=$(cat .changed)" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

      # ---------------- COMMIT CHANGES ----------------
      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add data.json .changed || true

          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git commit -m "Update directory data"
          git push

      # ---------------- COMMENT + CLOSE ISSUE ----------------
      - name: Comment and close issue
        if: steps.change_flag.outputs.changed == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const issue_number = context.payload.issue.number;

            const username =
              `${{ steps.parse.outputs.username || steps.parse_remove.outputs.username }}`;

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number,
              body: `✅ Processed successfully.\n\nUser: \`${username}\``
            });

            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number,
              state: "closed"
            });
